<%= render @booking %>

<div>
  <%= link_to "Edit this booking", edit_booking_path(@booking.booking_token) %> |
  <%= link_to "Back to bookings", bookings_path %>

  <%= button_to "Destroy this booking", booking_path(@booking.booking_token), method: :delete %>
</div>
