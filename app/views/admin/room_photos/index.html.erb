<h1>Room photos</h1>

<div id="room_photos">
  <% @room_photos.each do |room_photo| %>
    <%= render room_photo %>
    <p>
      <%= link_to "Show this room photo", admin_room_photo_path(room_photo) %>
    </p>
  <% end %>
</div>
