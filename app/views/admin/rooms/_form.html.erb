<%= form_with(model: @admin_room) do |form| %>
  <% if @admin_room.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(@admin_room.errors.count, "error") %> prohibited this room from being saved:</h2>

      <ul>
        <% @admin_room.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :room_name, style: "display: block" %>
    <%= form.text_field :room_name %>
  </div>

  <div>
    <%= form.label :short_description, style: "display: block" %>
    <%= form.text_area :short_description %>
  </div>

  <div>
    <%= form.label :long_description, style: "display: block" %>
    <%= form.text_area :long_description %>
  </div>

  <div>
    <%= form.label :price, style: "display: block" %>
    <%= form.text_field :price %>
  </div>

  <div>
    <%= form.submit %>
    <%#= button_to "Create Room", admin_rooms_path, method: :post, class: 'btn btn-primary' %>
  </div>
<% end %>
